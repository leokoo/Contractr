<h1>Listing Groups</h1>
 
<table>
  <tr>
    <th>Group name</th>
    <th>Group members</th>
  </tr>
 
  <% @groups.each do |group| %>
    <tr>
      <td><%= group.title %></td>
      <td><%= group.text %></td>
      <td><%= link_to 'Show', group_path(group) %></td>
    </tr>
  <% end %>
</table>